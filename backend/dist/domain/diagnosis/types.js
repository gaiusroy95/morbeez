export const INSUFFICIENT_EVIDENCE_LABEL = 'Insufficient evidence for AI diagnosis';
export function isValidDiagnosisSource(source) {
    return source === 'model' || source === 'vision' || source === 'verified_reuse';
}
//# sourceMappingURL=types.js.map