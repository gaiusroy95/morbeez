{
  "name": "@morbeez/api",
  "version": "0.1.0-m3",
  "private": true,
  "type": "module",
  "description": "Morbeez API — commerce, WhatsApp, AI Crop Doctor",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "npm run build:render",
    "build:api": "tsc",
    "build:render": "npm run install:field-pwa && npm run build:api && npm run build:field",
    "install:field-pwa": "npm install --prefix field-pwa --include=dev",
    "build:field": "npm run build --prefix field-pwa",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/**/*.test.ts",
    "lint": "eslint src --ext .ts",
    "db:types": "echo 'Generate types: supabase gen types typescript --local > src/types/database.ts'"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "@fastify/helmet": "^13.0.0",
    "@fastify/rate-limit": "^10.0.0",
    "@fastify/static": "^8.0.0",
    "@supabase/supabase-js": "^2.47.0",
    "@types/node": "^22.0.0",
    "dotenv": "^16.4.0",
    "fastify": "^5.0.0",
    "pino": "^9.0.0",
    "pino-pretty": "^13.0.0",
    "sharp": "^0.33.5",
    "typescript": "^5.6.0",
    "zod": "^3.23.0"
  },
  "optionalDependencies": {
    "mqtt": "^5.10.4"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
